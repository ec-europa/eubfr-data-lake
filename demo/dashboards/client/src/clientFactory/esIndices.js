const logs = `${process.env.REACT_APP_STAGE}-logs`;
const meta = `${process.env.REACT_APP_STAGE}-meta`;
const projects = `${process.env.REACT_APP_STAGE}-projects`;
const qualityReports = `${process.env.REACT_APP_STAGE}-quality-reports`;

export default { logs, meta, projects, qualityReports };

const allDemos = ['demo-dashboard-server', 'demo-dashboard-client'];

/**
 * Returns a list of demo applications available in EUBFR project.
 *
 * @returns {Array} List of all demo applications.
 */
module.exports = () => allDemos;
